<!-- activity_payment_confirmation.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center">

    <!-- Título de la pantalla -->
    <TextView
        android:id="@+id/txtConfirmationTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="¡Pago Exitoso!"
        android:textSize="24sp"
        android:textColor="@android:color/holo_green_dark"
        android:textStyle="bold"
        android:paddingBottom="20dp"/>

    <!-- Mensaje de confirmación -->
    <TextView
        android:id="@+id/txtConfirmationMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="¡Gracias por tu compra! El pago se ha procesado correctamente."
        android:textSize="18sp"
        android:paddingBottom="20dp"
        android:textAlignment="center"/>

    <!-- Detalles de la compra -->
    <TextView
        android:id="@+id/txtProductDetails"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Producto: Camisa Deportiva"
        android:textSize="18sp"
        android:paddingBottom="10dp"/>

    <TextView
        android:id="@+id/txtPrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Precio: $15.00 USD"
        android:textSize="18sp"
        android:paddingBottom="20dp"/>

    <!-- Botón para regresar o continuar -->
    <Button
        android:id="@+id/btnBackToHome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Regresar a la tienda"
        android:textSize="18sp"
        android:textColor="@android:color/white"
        android:padding="10dp"/>
</LinearLayout>
